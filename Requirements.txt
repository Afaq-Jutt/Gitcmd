Git Commands:

1-git config --global user.email 'your_email'
2-git config --global user.name 'your_name'
3-git init
4-git add [Filename] || -A
5-git status
6-git commit -m 'Reauirements.txt'
7-git remote add origin reponame eg:-(https://github.com/Afaq-Jutt/Gitcmd.git)
8-git push origin master
9-git clone reponame
10-git pull origin master


//if push cmd don't work correctly then do it.

1-git pull origin master
2-git pull --rebase origin master
then:
3-git push origin master